---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: nifiregistries.bigdata.quay.io
spec:
  group: bigdata.quay.io
  names:
    kind: NifiRegistry
    listKind: NifiRegistryList
    plural: nifiregistries
    singular: nifiregistry
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NifiRegistry is the Schema for the nifi registries API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NifiRegistrySpec defines the desired state of Nifi Registry
            properties:
              expose:
                type: boolean
              image:
                description: Image the container image for the Nifi deployment
                type: string
            required:
            - expose
            type: object
          status:
            description: NifiStatus defines the observed state of Nifi
            properties:
              uiRoute:
                description: UI Route reference
                type: string
            required:
            - uiRoute
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
